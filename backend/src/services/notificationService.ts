export const sendEmailNotification = async (userId: number, message: string) => {
    // Simulate sending an email
    console.log(`Sending email to user ${userId}: ${message}`);
  };
  
  export const sendPushNotification = async (userId: number, message: string) => {
    // Simulate sending a push notification
    console.log(`Sending push notification to user ${userId}: ${message}`);
  };
  
  export const sendSMSNotification = async (userId: number, message: string) => {
    // Simulate sending an SMS
    console.log(`Sending SMS to user ${userId}: ${message}`);
  };
  